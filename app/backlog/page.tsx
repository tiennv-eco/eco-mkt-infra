import StubPage from '@/components/StubPage';
export default function BacklogPage() {
  return (
    <StubPage
      breadcrumb="Backlog & Requests"
      title="Backlog & Requests"
      description="Incoming marketing requests, active work, and routing decisions."
      phase="phase-3"
      phaseLabel="Phase 3 · Planned"
    />
  );
}
