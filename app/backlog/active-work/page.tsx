import StubPage from '@/components/StubPage';
export default function ActiveWorkPage() {
  return (
    <StubPage
      breadcrumb="Backlog & Requests › Active Work"
      title="Active Work"
      description="Requests currently in progress — status, owner, and expected delivery."
      phase="phase-3"
      phaseLabel="Phase 3 · Planned"
    />
  );
}
